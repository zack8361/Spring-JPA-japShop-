spring:
#  datasource:
#    url: jdbc:h2:mem:test << Test 에서는 Default 로 지정 되어 있다.
#    username: sa
#    password :
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop << Default 로 지정 되어 있다.
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace